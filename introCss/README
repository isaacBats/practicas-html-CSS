Que es lo que se ha visto en esta clase;

	La estructura del HTML y la importancia de el contenido 
	definido desde un principio.

	Como y los diferentes tipos de selectores en CSS.

	Como implementar cada uno de los selectores.

	Como crear un modulo en CSS.

	Como crear nustros propios selectores de atributo.

	Como dar estilo en base al estado que los atributos puedan tener

	Anidar selectores de atributo

